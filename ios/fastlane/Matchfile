git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")
type("appstore")
app_identifier(["com.yela.app"])
username(ENV["APPLE_ID"] || "are.atar.are@gmail.com")

# Use wildcard for initial setup
# app_identifier(["*"])
